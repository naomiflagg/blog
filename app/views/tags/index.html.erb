<h1>All tags</h1>

<ul id='tags'>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), class: 'tag_name', id: "tag_#{tag.id}" %>
    </li>
  <% end %>
</ul>

<p><%= link_to 'View all articles', articles_path%></p>

